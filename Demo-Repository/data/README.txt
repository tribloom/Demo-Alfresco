This directory is where any additional data necessary for your project will be
placed. An example may be data that is pre-loaded into the system when it is 
deployed via an XML file. An concrete example would be users and groups for
your project.